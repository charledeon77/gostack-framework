# ─── GoStack Framework .gitignore ─────────────────────────────────────────────
#
# This is the GoStack *framework* library repository.
# Application-level directories below are generated by CLI commands such as
# `make:migration`, `make:controller`, and `make:auth` when developers run
# the GoStack CLI from within *their own* application projects.
#
# They must NEVER be committed into the framework source tree.
# ──────────────────────────────────────────────────────────────────────────────

# Real environment secrets — never commit.
# The safe reference template (.env.example) IS committed and tracked.
.env

# Generated by `make:migration` / `make:auth`
/database/

# Generated by `make:controller` / `make:auth`
/internal/

# Go build artifacts
*.exe
*.test
*.out

# Go workspace file (optional local overrides)
go.work
go.work.sum
